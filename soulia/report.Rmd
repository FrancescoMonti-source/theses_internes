---
title: "Thèse Sara S."
author: "Francesco MONTI"
date: "`r Sys.Date()`"
output: 
  html_document: 
    toc: true
    toc_float: true
editor_options: 
  
  chunk_output_type: console
---

```{r setup, include=FALSE}
library(magrittr) # Operator %>% and additional pipe-friendly functions.
library(tidyverse) # The main "tidyverse" packages.
library(openxlsx)
library(ggplot2)
library(DT)
library(conflicted)
library(knitr)
library(kableExtra)
library(fmckage)
library(htmltools)
library(MASS)

knitr::opts_chunk$set(
  echo = FALSE, # Should blocks with program code be shown in knitted documents?
  eval = TRUE, # Should program code be evaluated?
  fig.height = 6, # Default height for plots.
  fig.width = 10, # Default width for plots.
  fig.align = "center", # Default alignment for plots in knitted documents.
  warning = FALSE,
  message = FALSE
)

theme_set(
  theme_bw() +
    theme(
      axis.text = element_text(face = "bold"),
      axis.title = element_text(face = "bold"),
      plot.title = element_text(face = "bold")
    )
) # Default ggplot2 theme

conflict_prefer("filter", "dplyr")
conflict_prefer("lag", "dplyr")
conflict_prefer("select","dplyr")

```

```{r opts dt}
options(
  DT.options = list(

    ## 🔢 Pagination & General Display
    pageLength     = 10,                                     # rows per page on load
    lengthMenu     = list(c(10, 25, 50, -1), c(10, 25, 50, "All")),  # dropdown menu
    fixedHeader    = TRUE,                                   # fix header on scroll
    autoWidth      = FALSE,                                   # auto-calculate column widths
    deferRender    = TRUE,                                   # better perf on large tables
    searching      = TRUE,                                   # global search box
    searchHighlight = TRUE,                                  # highlight matched terms
    paging         = TRUE,                                   # enable pagination
    info           = TRUE,                                   # show "Showing 1 to X of Y"
    
    ## 🔁 Scrolling
    scrollX        = TRUE,                                   # horizontal scroll
    scrollY        = "400px",                                # vertical scroll
    scrollCollapse = TRUE,                                   # collapse unused scroll area
    
    ## 📌 Layout and Buttons
    dom            = "Blfrtip",                              # controls layout
    
    ## 🖋 Header style (JS)
    initComplete = JS(
      "function(settings, json) {",
      "$(this.api().table().header()).css({'font-weight':'bold','text-align':'center'});",
      "}"
    )
    
    ## 🧱 Column definitions (optional – uncomment and modify as needed)
    # columnDefs = list(
    #   list(visible = FALSE, targets = 0),                    # hide first column
    #   list(orderable = FALSE, targets = 1),                  # make column unsortable
    #   list(className = "dt-center", targets = 2),            # center align
    #   list(className = "dt-right",  targets = c(3, 4)),      # right align
    #   list(width = "100px", targets = 5)                     # fixed width
    # ),
    
    ## 🔡 Initial sorting (optional)
    # order = list(list(1, 'asc')),                            # sort by column 2 (0-indexed JS)

    ## 📂 Row grouping (optional)
    # rowGroup = list(dataSrc = 1)                             # group by 2nd column (0-indexed)
    # rowGroup = list(dataSrc = c(1, 2))                       # multiple group levels
  )
)
```

```{r load data, include=FALSE}
data <- read.xlsx("tableau_clean.xlsx", sheet = 1) %>%
  as_tibble() %>%
  select(id:commentaires)
```

```{r some cleaning}
# Patient vide
data <- data[-204, ]
data$tabac[which(data$tabac == 0)] <- "Jamais"
data <- data %>%
  mutate(across(where(is.character), function(x) str_squish(x)),
    across(everything(), function(x) ifelse(as.character(x) == "", NA, x)),
    across(everything(), function(x) ifelse(as.character(x) == "?", NA, x)),
    taille = as.numeric(str_replace(taille, ",", ".")),
    taille = case_when(
      taille < 10 ~ taille * 100,
      taille < 100 ~ taille + 100,
      TRUE ~ taille
    ),
    imc = round(as.numeric(poids) / (as.numeric(taille) / 100)^2, 2),
    sero_vih_t1 = ifelse(is.na(sero_vih_t1), 0, sero_vih_t1),
    sero_vhc_t1 = ifelse(is.na(sero_vhc_t1), 0, sero_vhc_t1),
    cv_detectable_1 = case_when(
      !is.na(reduction_cv_vhd_1) & is.na(cv_detectable_1) ~ 0,
      TRUE ~ cv_detectable_1
    ),
    cirrhose_bin = ifelse(complications_cirrhose != "Non", 1, 0),
    oh = ifelse(oh %in% c("Jamais", "Non sevré", "Sevré"), oh, NA)
  )

data[75,c("taille","poids","imc")] = NA

data$oh[which(data$oh == "Jamais")] <- "Non"
data$tabac[which(data$tabac == "Jamais")] <- "Non"

data$oh[which(data$oh == "Non sevré")] <- "Actif"
data$tabac[which(data$tabac == "Non sevré")] <- "Actif"
```

```{r fibrose cleaning}
clean_stage_col <- function(x) {
  x <- str_trim(x)
  x <- str_replace_all(x, ",", ".") # convert comma decimal
  x <- na_if(x, "") # blank <U+2192> NA
  x <- na_if(x, " ") # trailing space <U+2192> NA
  return(x)
}

data <- data %>%
  mutate(
    fibrotest_baseline     = clean_stage_col(fibrotest_baseline),
    fibroscanner_baseline  = clean_stage_col(fibroscanner_baseline)
  )

extract_numeric <- function(x) parse_number(x)
extract_stage <- function(x) str_extract(str_to_upper(x), "\\bF[0-4]\\b")

convert_fibrotest_stage <- function(score) {
  case_when(
    is.na(score) ~ NA_character_,
    score < 0.21 ~ "F0",
    score < 0.27 ~ "F1",
    score < 0.49 ~ "F2",
    score < 0.74 ~ "F3",
    score >= 0.74 ~ "F4"
  )
}

data <- data %>%
  mutate(
    fibrotest_score = extract_numeric(fibrotest_baseline),
    fibroscan_score = extract_numeric(fibroscanner_baseline),
    fibrotest_baseline = coalesce(
      extract_stage(fibrotest_baseline),
      convert_fibrotest_stage(fibrotest_score)
    ),
    fibroscan_stage = extract_stage(fibroscanner_baseline),
    fib4_baseline = case_when(
      is.na(fib4_baseline) ~ NA_character_,
      fib4_baseline < 1.0 ~ "F0–F1",
      fib4_baseline < 1.3 ~ "F1–F2",
      fib4_baseline < 2.0 ~ "F2–F3",
      fib4_baseline <= 2.67 ~ "F3",
      fib4_baseline > 2.67 ~ "F4",
      TRUE ~ fib4_baseline
    )
  )


data <- data %>%
  mutate(score_fibrose = do.call(coalesce, across(pbh_hepatectomie_baseline:fib4_baseline)))

data$score_fibrose <- na_if(data$score_fibrose, " ")

data <- data %>%
  mutate(
    score_fibrose = factor(score_fibrose, levels = c("F0", "F1", "F2", "F3", "F4"), ordered = TRUE),
    oh = factor(oh, levels = c("Non", "Sevré", "Actif")),
    tabac = factor(tabac, levels = c("Non", "Sevré", "Actif")),
    masld = factor(masld),
    diabete = factor(diabete),
    sero_vhc_t1 = factor(sero_vhc_t1),
    sero_vih_t1 = factor(sero_vih_t1)
  )
```

# Exploration données

## Dimensions

`r nrow(data)` patients, `r ncol(data)` colonnes.

## Données manquantes

Pour certaines variables, valeur manquante = "absence" très probablement, pour d'autre c'est plus difficile à dire. Dans tous les cas, scientifiquement ce n'est pas une position idéale.

```{r tableau valeurs manquantes}
apply(data, 2, function(x) sum(is.na(x))) %>%
  as.data.frame() %>%
  rownames_to_column() %>%
  `colnames<-`(c("variable", "valeurs_manquantes")) %>%
  mutate(pourcentage = round(valeurs_manquantes * 100 / nrow(data), 2)) %>%
  datatable(
    caption = htmltools::tags$caption(
      style = "caption-side: top; text-align: left;
                                     font-size: 18px; font-weight: bold;
                                     padding: 10px;",
      "Valeurs manquantes par variable"
    ),
    options = list(
      buttons = list(
        "copy",
        list(extend = "excel", filename = "tableau_valeurs_manquantes")
      )
    )
  )
```

```{r NA par variable et ville}
# need to fix chu labels
# Define caption (and reuse for filename)
caption_text <- "Valeurs manquantes par variable et ville"
data %>%
  group_by(ville_suivi) %>%
  summarise(
    across(everything(), ~ sum(is.na(.))),
    nb_patients = n()
  ) %>%
  rowwise() %>%
  mutate(
    na_count = sum(c_across(where(is.numeric))),
    na_count_per_row = na_count / nb_patients
  ) %>%
  relocate(na_count, .after = ville_suivi) %>%
  relocate(nb_patients, .after = ville_suivi) %>%
  relocate(na_count_per_row, .after = na_count) %>%
  arrange(desc(na_count_per_row)) %>%
  mutate( # na_pourcentage = na_count*100/(nb_patients*(ncol(.)-2)),
    na_pourcentage = na_count * 100 / (nb_patients * (ncol(data) - 2)),
    across(where(is.numeric), function(x) round(x, 2))
  ) %>%
  relocate(na_pourcentage, .after = na_count_per_row) %>%
  datatable(
    caption = htmltools::tags$caption(
      style = "caption-side: top; text-align: left;
                        font-size: 18px; font-weight: bold;
                        padding: 10px;",
      caption_text
    ),
    extensions = "Buttons",
    options = list(
      buttons = list(
        "copy",
        list(extend = "excel", filename = caption_text)
      )
    )
  )
```

# Analyse

## Sexe & age

```{r sexe et age}
# Define caption (and reuse for filename)
caption_text <- "Sexe"

data %>%
  ffreq("sexe") %>%
  select(-Variable) %>%
  datatable(
    caption = tags$caption(
      style = "caption-side: top; text-align: left; font-size: 18px;
               font-weight: bold; padding: 10px;",
      caption_text
    ),
    extensions = "Buttons",
    options = list(
      pageLength = 10,
      scrollX = TRUE,
      scrollY = TRUE,
      dom = "Bfrtip",
      buttons = list(
        "copy",
        list(extend = "excel", filename = caption_text)
      )
    )
  )


# Define caption (and reuse for filename)
caption_text <- "Age by Sexe"
fmckage::fdescribe(data, "age", "sexe") %>%
  datatable(
    caption = tags$caption(
      style = "caption-side: top; text-align: left; font-size: 18px;
        font-weight: bold; padding: 10px;",
      caption_text
    ),
    extensions = "Buttons",
    options = list(
      buttons = list(
        "copy",
        list(extend = "excel", filename = caption_text)
      )
    )
  )
```

## Age diag VHD

```{r age au diag de VHD}
# Define caption (and reuse for filename)
caption_text <- "Age au diag VHD"
data %>%
  group_by(sexe) %>%
  summarise(
    mean =
      mean(age_diag_vhd, na.rm = TRUE),
    sd = sd(age_diag_vhd, na.rm = TRUE),
    min = min(age_diag_vhd, na.rm = TRUE),
    max = max(age_diag_vhd, na.rm = TRUE),
    range = max(age_diag_vhd, na.rm = TRUE) - min(age_diag_vhd, na.rm = TRUE),
    se = sd(age_diag_vhd, na.rm = TRUE) / sqrt(n())
  ) %>%
  ungroup() %>%
  mutate(across(where(is.numeric), function(x) round(x, 2))) %>%
  datatable(
    caption = tags$caption(
      style = "caption-side: top; text-align: left; font-size: 18px;
               font-weight: bold; padding: 10px;",
      caption_text
    ),
    extensions = "Buttons",
    options = list(
      buttons = list(
        "copy",
        list(extend = "excel", filename = caption_text)
      )
    )
  )
```

## OH

```{r alcool}
# Define caption (and reuse for filename)
caption_text <- "Consommation OH by sexe"

data %>%
  count(oh, sexe) %>%
  group_by(sexe) %>%
  mutate(pourcentage_by_sexe = round(n * 100 / sum(n), 2)) %>%
  arrange(sexe) %>%
  ungroup() %>%
  mutate(pourcentage_global = round(n * 100 / sum(n), 2)) %>%
  datatable(
    caption = tags$caption(
      style = "caption-side: top; text-align: left; font-size: 18px;
               font-weight: bold; padding: 10px;",
      caption_text
    ),
    extensions = c("Buttons", "RowGroup"),
    options = list(
      pageLength = 10,
      lengthMenu = list(c(10, 25, 50, -1), c(10, 25, 50, "All")),
      dom = "Blfrtip",
      buttons = list(
        "copy",
        list(extend = "excel", filename = caption_text)
      ),
      # -- make ALL headers bold and x-aligned -----------------------------
      initComplete = JS(
        "function(settings, json) {",
        "$(this.api().table().header()).css({'font-weight':'bold','text-align':'right'});",
        "}"
      ),
      rowGroup = list(
        dataSrc = 2
      ),
      columnDefs = list(
        list(
          visible = FALSE,
          targets = 2
        )
      )
    ), 
    rownames = rep("",8),
    colnames = c('Consommation OH',"Sex", 'N', '% by sex', '% global')
  )
```

## Biologie
### Cirrhotiques vs non
```{r resultats biologie by cirrhose}
# Pour les résultats biologiques, Séparer la moyenne des bio des patients cirrhotiques et non cirrhotiques ?

bio_vars <- c("asat", "alat", "ggt", "bt", "tp", "plaq", "albu")

caption_text <- "Bio cirrhotiques vs non"

bio_stats <- data %>%
  mutate(
    cirrhose_bin = factor(cirrhose_bin), # 0 / 1 come fattore
    across(all_of(bio_vars), as.numeric) # assicura numerico
  ) %>%
  pivot_longer(
    cols = all_of(bio_vars),
    names_to = "test_bio",
    values_to = "value"
  ) %>%
  group_by(test_bio, cirrhose_bin) %>%
  summarise(
    mean = mean(value, na.rm = TRUE),
    sd = sd(value, na.rm = TRUE),
    median = median(value, na.rm = TRUE),
    min = min(value, na.rm = TRUE),
    max = max(value, na.rm = TRUE),
    range = max - min,
    .groups = "drop"
  ) %>%
  # --- NEW STEP: round all numeric columns to 2 decimals ---
  mutate(across(where(is.numeric), ~ round(.x, 2)))

# vignette("DT", package = "DT")

DT::datatable(
  rownames = rep("",21),
  data = bio_stats,
  caption = tags$caption(
    style = "caption-side: top; text-align: left; font-size: 18px; font-weight: bold; padding: 10px;",
    "Stats biologie"
  ),
  extensions = c("Buttons", "RowGroup", "FixedHeader"),
  options = list(
    # -- make ALL headers bold and right-aligned -----------------------------
    initComplete = JS(
      "function(settings, json) {",
      "  $(this.api().table().header()).css({'font-weight':'bold','text-align':'right'});",
      "}"
    ),
    buttons = list(
      "copy",
      list(extend = "excel", filename = "Stats biologie")
    ),
    rowGroup = list(
      dataSrc = match("test_bio", names(bio_stats))
    ),
    columnDefs = list(
      list(
        visible = FALSE,
        targets = match("test_bio", names(bio_stats))
      ),
      list(
        className = "dt-right",
        targets = na.omit(match(
          names(bio_stats)[sapply(bio_stats, is.numeric)],
          names(bio_stats)
        ))
      )
    ),
    order = list(list(1, "asc"))
        
  )
) |>
  DT::formatRound(
    columns = names(bio_stats)[sapply(bio_stats, is.numeric)],
    digits = 2
  ) #|>
# DT::formatStyle(
#   columns = names(bio_stats),
#   fontWeight = "bold",
#   `text-align` = "right"
# )
```
### Cytolise - stade fibrose
```{r boxplot asat stade fibrose}
g = ggplot(data, aes(x = score_fibrose, y = as.numeric(asat))) +
    geom_boxplot(aes(fill = score_fibrose)) +
        scale_y_continuous(breaks = seq(0,1000,50))+
    labs(title = "ASAT par stade de fibrose", x = "Score de fibrose", y = "ASAT (U/L)")
g
save_editable_plot(g, "boxplot_asat_score_fibrose", path = "graphs")
```
[📥 Télécharger le graphique (PPTX)](graphs/boxplot_asat_score_fibrose.pptx)  

```{r boxplot alat stade fibrose}
g = ggplot(data, aes(x = score_fibrose, y = as.numeric(alat))) +
        geom_boxplot(aes(fill = score_fibrose)) +
        scale_y_continuous(breaks = seq(0,1000,50))+
        labs(title = "ALAT par stade de fibrose", 
             x = "Score de fibrose", 
             y = "ALAT (U/L)") 
g
save_editable_plot(g, "boxplot_aLat_score_fibrose", path = "graphs")

```
[📥 Télécharger le graphique (PPTX)](graphs/boxplot_alat_score_fibrose.pptx)

Stades de fibrose plus élévés semblent avoir en moyenne des valeurs plus hautes mais il y a beaucoup de superposition entre les boites à moustache donc dèjà grafiquement on voit que ce n'est pas significatif. On a quand même 172/267 valeurs manquantes en plus.


## Mode transmission

```{r mode transmission}
# Deux profils de patients très différents :
# o   Originaires de zones d'endémie : Pas de comorbidités, pas de consommation éthylo tabagique
# o   Caucasiens : OH-Tabac associé, toxicomanie plus fréquente, confection VHC/VIH plus fréquente ?
data$vhd_mode_transmission[which(data$vhd_mode_transmission == "Maternofoetal")] <- "Materno-foetal"

data %>%
  select(id, age, ethnie, vhd_mode_transmission, oh, tabac, vhd_evolution_ans, cirrhose_bin) %>%
  count(vhd_mode_transmission, sort = TRUE) %>%
  mutate("%" = round(n * 100 / sum(n), 2)) %>%
  DT::datatable(
          rownames = FALSE,
    caption = tags$caption(
      style = "caption-side: top; text-align: left; font-size: 18px; font-weight: bold; padding: 10px;",
      "Mode transmission"
    ),
    extensions = c("Buttons", "RowGroup", "FixedHeader"),
    options = list(
      buttons = list(
        "copy",
        list(extend = "excel", filename = "Mode transmission")
      ),
      # -- make ALL headers bold and x-aligned -----------------------------
      initComplete = JS(
        "function(settings, json) {",
        "$(this.api().table().header()).css({'font-weight':'bold','text-align':'right'});",
        "}"
      )
    )
  ) |>
  DT::formatRound(
    columns = "%",
    digits = 2
  )
```

```{r mode transmission by ethnie}
# Deux profils de patients très différents :
# o   Originaires de zones d'endémie : Pas de comorbidités, pas de consommation éthylo tabagique
# o   Caucasiens : OH-Tabac associé, toxicomanie plus fréquente, confection VHC/VIH plus fréquente ?
data %>%
  select(id, age, ethnie, vhd_mode_transmission, oh, tabac, vhd_evolution_ans, cirrhose_bin) %>%
  group_by(ethnie) %>%
  count(vhd_mode_transmission) %>%
  arrange(ethnie, desc(n)) %>%
  mutate("%_by_ethnie" = round(n * 100 / sum(n), 2)) %>%
  DT::datatable(
          rownames = rep("",21),
    caption = tags$caption(
      style = "caption-side: top; text-align: left; font-size: 18px; font-weight: bold; padding: 10px;",
      "Mode transmission by ethnie"
    ),
    extensions = c("Buttons", "RowGroup", "FixedHeader"),
    options = list(
      buttons = list(
        "copy",
        list(extend = "excel", filename = "Mode transmission by ethnie")
      ),
      # -- make ALL headers bold and x-aligned -----------------------------
      initComplete = JS(
        "function(settings, json) {",
        "$(this.api().table().header()).css({'font-weight':'bold','text-align':'right'});",
        "}"
      ),
      rowGroup = list(
        dataSrc = match("ethnie", names(.))
      ),
      columnDefs = list(
        list(
          visible = FALSE,
          targets = match("ethnie", names(.))
        ),
        list(
          className = "dt-right",
          targets = na.omit(match(
            names(.)[sapply(., is.numeric)],
            names(.)
          ))
        )
      ),
      order = list(list(1, "asc"))
    )
  ) |>
  DT::formatRound(
    columns = "%_by_ethnie",
    digits = 2
  )
```

```{r Mode transmission and comorbidites coinfections by ethnie}
# Deux profils de patients très différents :
# o   Originaires de zones d'endémie : Pas de comorbidités, pas de consommation éthylo tabagique
# o   Caucasiens : OH-Tabac associé, toxicomanie plus fréquente, confection VHC/VIH plus fréquente ?
data %>%
  select(id, age, ethnie, vhd_mode_transmission, oh, tabac, vhd_evolution_ans, cirrhose_bin, diabete, hta, masld, sero_vhc_t1, sero_vih_t1) %>%
  group_by(ethnie) %>%
  count(vhd_mode_transmission, oh, tabac, sero_vhc_t1, sero_vih_t1) %>%
  arrange(ethnie, desc(n)) %>%
  mutate(
    "%_by_ethnie" = round(n * 100 / sum(n), 2),
    n = paste0(n, "/", sum(n))
  ) %>%
  ungroup() %>%
  DT::datatable(
          rownames = rep("",101),
          filter = list(position = "top"),
    caption = tags$caption(
      style = "caption-side: top; text-align: left; font-size: 18px; font-weight: bold; padding: 10px;",
      "Mode transmission & comorbidités/co-infections by ethnie"
    ),
    extensions = c("Buttons", "RowGroup", "FixedHeader"),
    options = list(
      buttons = list(
        "copy",
        list(extend = "excel", filename = "Mode transmission & comorbidités/co-infections by ethnie")
      ),
      # -- make ALL headers bold and x-aligned -----------------------------
      initComplete = JS(
        "function(settings, json) {",
        "$(this.api().table().header()).css({'font-weight':'bold','text-align':'right'});",
        "}"
      ),
      rowGroup = list(
        dataSrc = 1
      ),
      order = list(list(1, "asc"))
    )
  ) |>
  DT::formatRound(
    columns = "%_by_ethnie",
    digits = 2
  )
```

## Fibrose hépatique

```{r distribution score fibrose}
g <- data %>%
  count(score_fibrose, name = "count") %>%
  mutate(
    percent = round(count * 100 / sum(count), 1),
    label   = paste0(count, "\n(", percent, "%)")
  ) %>%
  ggplot(aes(x = score_fibrose, y = count)) +
  geom_col(fill = "steelblue") +
  geom_text(aes(label = label, y = count - 20), vjust = -0.5, fontface = "bold") +
  scale_y_continuous(
    expand = expansion(mult = c(0, 0.1)),
    breaks = seq(0, 200, 10)
  ) +
  labs(
    x = "Stage fibrose",
    y = "Count",
    title = "Distribution du score de fibrose"
  ) +
  theme_minimal(base_size = 13) +
  theme(
    axis.text  = element_text(face = "bold"),
    axis.title = element_text(face = "bold"),
    plot.title = element_text(face = "bold", hjust = 0.5)
  )

save_editable_plot(g, "distribution_score_fibrose", path = "graphs")

g
```

[📥 Télécharger le graphique (PPTX)](graphs/distribution_score_fibrose.pptx)

```{r score fibrose by mode transmission}
data %>%
  select(id, score_fibrose, vhd_mode_transmission, vhd_evolution_ans, pbh_hepatectomie_baseline, matches("fib.*_baseline")) %>%
  group_by(vhd_mode_transmission, score_fibrose) %>%
  summarise(n = n(), "%" = round(n * 100 / nrow(.), 2), tmp_moyen_depuis_diag_vhd_ans = round(mean(vhd_evolution_ans, na.rm = T), 2)) %>%
  arrange(vhd_mode_transmission, desc(n)) %>%
  DT::datatable(
          rownames = rep("",26),
          filter = list(position = 'top'),
    caption = tags$caption(
      style = "caption-side: top; text-align: left; font-size: 18px; font-weight: bold; padding: 10px;",
      "Score fibrose by mode transmission et evolution"
    ),
    extensions = c("Buttons", "RowGroup", "FixedHeader"),
    options = list(
      buttons = list(
        "copy",
        list(extend = "excel", filename = "Score fibrose by mode transmission et evolution")
      ),
      # -- make ALL headers bold and x-aligned -----------------------------
      initComplete = JS(
        "function(settings, json) {",
        "$(this.api().table().header()).css({'font-weight':'bold','text-align':'right'});",
        "}"
      ),
      rowGroup = list(
        dataSrc = match("vhd_mode_transmission", names(.))
      ),
      list(
        className = "dt-right",
        targets = na.omit(match(
          names(.)[sapply(., is.numeric)],
          names(.)
        ))
      )
    )
    # order = list(list(1, 'asc'))
  )
```

### Associations
```{r }


library(tableone)

# Define variables
vars <- c("oh", "tabac", "masld", "diabete", "sero_vhc_t1", "sero_vih_t1", "imc", "vhd_evolution_ans")
cat_vars <- c("oh", "tabac", "masld", "diabete", "sero_vhc_t1", "sero_vih_t1")

# Create the table stratified by fibrosis
tab <- CreateTableOne(vars = vars, strata = "score_fibrose", data = data, factorVars = cat_vars, includeNA = TRUE)

# Step 2: Convert to data.frame
tab_df <- print(tab, test = TRUE, quote = FALSE, noSpaces = TRUE, printToggle = FALSE) %>%
  as.data.frame()

# Step 3: Add variable names as a column
tab_df <- tab_df %>%
  tibble::rownames_to_column(var = "Variable") %>%
  mutate(Variable = str_remove_all(Variable, "\\.|X"))

# Optional: Fix rownames or indent levels
tab_df$Variable <- c("n", "oh", "- Non", "- Sevré", "- Actif", "- NA", "tabac", "- Non", "- Sevré", "- Actif", "- NA", "masld", "- 0", "- 1", "- NA", "diabete", "- 0", "- 1", "- NA", "sero_vhc_t1", "sero_vih_t1", "imc(mean-SD)", "vhd_evolution_ans(mean-SD)")

# Step 4: Display as DataTable with download buttons
datatable(
  tab_df,
  extensions = "Buttons",
  options = list(
    buttons = c("copy", "excel")
  ),
  rownames = FALSE,
  caption = tags$caption(
    style = "caption-side: top; text-align: left; font-size: 18px; font-weight: bold; padding: 10px;",
    "Associations variables et score de fibrose"
  )
)
# Bold variable names
```

🧰 Ce que nous avons fait

Nous avons réalisé une **analyse descriptive et bivariée** afin d’explorer les associations entre le **score de fibrose hépatique** (allant de F0 à F4) et plusieurs variables cliniques, biologiques et comportementales.

Pour cela, nous avons : - Stratifié l’échantillon selon le **stade de fibrose** (`score_fibrose`) ;

-   Calculé, pour chaque variable :
    -   La **répartition en pourcentage** des modalités pour les variables qualitatives (`oh`, `tabac`, `masld`, `diabete`, `sero_vhc_t1`, `sero_vih_t1`) ;
    -   La **moyenne et l’écart-type** pour les variables quantitatives (`imc`, `vhd_evolution_ans`) ;
-   Utilisé des **tests statistiques appropriés** pour comparer les groupes :
    -   Le **test du Chi²** (ou Fisher exact) pour les variables qualitatives ;
    -   Le **test de Kruskal-Wallis** pour les variables quantitatives.


📊 Interprétation des résultats

-   **Consommation d’alcool (`oh`)**\
    Nous avons observé une **association significative** entre la consommation d’alcool et le stade de fibrose (p = 0.008). Les patients ayant une consommation sévère étaient plus fréquemment représentés aux stades avancés (notamment F3), suggérant un rôle possible de l’alcool dans la progression de la fibrose. Ce qui colle avec ce que nous savons dèjà.

-   **Tabac (`tabac`)**\
    Nous n’avons pas mis en évidence d’association significative entre le tabagisme et le stade de fibrose (p = 0.662). La proportion de fumeurs actifs reste relativement stable entre les différents stades.

-   **MASLD (`masld`)**\
    Nous avons constaté une prévalence légèrement plus élevée du MASLD dans les stades F3 et F4, mais sans que cette différence ne soit statistiquement significative (p = 0.244).

-   **Diabète (`diabete`)**\
    Aucune association significative n’a été observée entre la présence d’un diabète et la sévérité de la fibrose (p = 0.518).

-   **Séropositivité au VHC (`sero_vhc_t1`)**\
    Nous avons observé une **tendance** à une association entre la séropositivité au VHC et des stades plus élevés de fibrose (p = 0.056). Cette tendance suggère un possible effet du VHC sur la progression, bien que la significativité statistique ne soit pas atteinte.

-   **Séropositivité au VIH (`sero_vih_t1`)**\
    Nous n’avons pas observé de différence significative en fonction du statut VIH (p = 0.870).

-   **Indice de masse corporelle (`imc`)**\
    L’IMC moyen ne varie pas de manière significative selon le stade de fibrose (p = 0.106). 

-   **Durée d’évolution d’une maladie hépatique (`vhd_evolution_ans`)**\
    Nous avons observé une **association fortement significative** entre la durée d’évolution et le stade de fibrose (p \< 0.001). Les patients aux stades F2 à F4 présentaient une durée médiane d’évolution deux fois plus longue que ceux aux stades F0/F1, ce qui soutient un lien temporel entre la chronicité de l’atteinte hépatique et la progression de la fibrose ce qui est cohérent. 
    
Les résultats sont similaires si on test cirrhose oui/non plutot que le stage:
```{r}
tab = CreateTableOne(vars = vars, strata = "cirrhose_bin", data = data, factorVars = cat_vars, includeNA = TRUE)

# Step 2: Convert to data.frame
tab_df <- print(tab, test = TRUE, quote = FALSE, noSpaces = TRUE, printToggle = FALSE) %>%
  as.data.frame()

# Step 3: Add variable names as a column
tab_df <- tab_df %>%
  tibble::rownames_to_column(var = "Variable") %>%
  mutate(Variable = str_remove_all(Variable, "\\.|X"))

# Optional: Fix rownames or indent levels
tab_df$Variable <- c("n", "oh", "- Non", "- Sevré", "- Actif", "- NA", "tabac", "- Non", "- Sevré", "- Actif", "- NA", "masld", "- 0", "- 1", "- NA", "diabete", "- 0", "- 1", "- NA", "sero_vhc_t1", "sero_vih_t1", "imc(mean-SD)", "vhd_evolution_ans(mean-SD)")

# Step 4: Display as DataTable with download buttons
datatable(
  tab_df,
  extensions = "Buttons",
  options = list(
    buttons = c("copy", "excel")
  ),
  rownames = FALSE,
  caption = tags$caption(
    style = "caption-side: top; text-align: left; font-size: 18px; font-weight: bold; padding: 10px;",
    "Associations variables et cirrhose oui/non"
  )
)
data$vhdcv
```

## Characteristiques viro
```{r}
g = data %>% 
        select(id,score_fibrose,vhd_cv_pre_traitement) %>%
        mutate(vhd_cv_pre_traitement = as.numeric(vhd_cv_pre_traitement)) %>% 
        ggplot()+
        geom_boxplot(aes(x = score_fibrose, y = vhd_cv_pre_traitement, fill = score_fibrose))+
        coord_cartesian(ylim = c(0, 12))+
        geom_point(aes(x = score_fibrose, y = vhd_cv_pre_traitement), alpha = .2, size = 5)+
        scale_y_continuous(breaks = seq(0,20,2))+
        labs(title = "Charge virale vs fibrose au diagnostique",
             y = "CV au diagnostique",
             x = "Stade fibrose au diagnostique"
             )

g
save_editable_plot(g,"cv_vs_fibrose_baseline", path = "graphs")
```

[📥 Télécharger le graphique (PPTX)](graphs/cv_vs_fibrose_baseline.pptx)

Beaucoup de superposition entre les boites des différents groupes. Si une différence existe elle n'est pas évidente mais c'est certain qu'avec tellement de données manquantes on manque de puissance statistique. La mediane des F0 et F1 est littéralement à 0 (cv non detectable) ce qui veut dire que au moins la moitié des gens dans ces groupes est "indetectable". C'est une piste mais on a pas les données pour faire plus.
Si on rajoute une stratification par cirrhose oui/non ça devient dramatiquement évident. 

```{r}
data %>% 
        select(id,score_fibrose,vhd_cv_pre_traitement,cirrhose_bin) %>%
        mutate(vhd_cv_pre_traitement = as.numeric(vhd_cv_pre_traitement),
               cirrhose_bin = factor(cirrhose_bin,labels = c("Non cirrhotique","Cirrhotique"))) %>% 
        ggplot()+
        geom_boxplot(aes(x = score_fibrose, y = vhd_cv_pre_traitement, fill = score_fibrose))+
        coord_cartesian(ylim = c(0, 10))+
        geom_point(aes(x = score_fibrose, y = vhd_cv_pre_traitement), alpha = .2, size = 5)+
        scale_y_continuous(breaks = seq(0,20,2))+
        labs(title = "Charge virale vs fibrose au diagnostique",
             y = "CV au diagnostique",
             x = "Stade fibrose au diagnostique"
             )+
        facet_wrap(facets = "cirrhose_bin")
```

### Distribution valeurs CV
```{r}
# Séparation des patients avec une CV négative et positive. Puis parmi les CV+, noter la moyenne des patients avec CV + ? 

# tableau distribution CV (0s inclus)

data %>% 
        mutate(vhd_cv_pre_traitement = as.numeric(vhd_cv_pre_traitement)) %>% 
        group_by(cirrhose_bin) %>%
        summarise(n = n(),
                  mean = mean(vhd_cv_pre_traitement,na.rm=T),
                  median = median(vhd_cv_pre_traitement,na.rm=T),
                  min = min(vhd_cv_pre_traitement,na.rm=T),
                  max = max(vhd_cv_pre_traitement,na.rm=T),
                  sd = sd(vhd_cv_pre_traitement,na.rm=T)) %>%
        mutate(across(where(is.numeric), function(x) round(x,2))) %>% 
        DT::datatable(
                caption = tags$caption(
                        style = "caption-side: top; text-align: left; font-size: 18px; font-weight: bold; padding: 10px;",
                        "Distribution CV (0s inclus)"
                ),
                rownames = FALSE,
                extensions = c("Buttons", "RowGroup", "FixedHeader"),
                options = list(buttons        = list(
                      "copy",
                      list(extend = "excel", filename = "tableau distribution CV (0s inclus)")
                      # Also available: "csv", "pdf", "print"
                )),
        )

# tableau distribution CV (0s exclus)
data %>% 
        mutate(vhd_cv_pre_traitement = as.numeric(vhd_cv_pre_traitement)) %>% 
        filter(vhd_cv_pre_traitement > 0 ) %>% 
        group_by(cirrhose_bin) %>%
        summarise(n = n(),
                  mean = mean(vhd_cv_pre_traitement,na.rm=T),
                  median = median(vhd_cv_pre_traitement,na.rm=T),
                  min = min(vhd_cv_pre_traitement,na.rm=T),
                  max = max(vhd_cv_pre_traitement,na.rm=T),
                  sd = sd(vhd_cv_pre_traitement,na.rm=T)) %>%  
        mutate(across(where(is.numeric), function(x) round(x,2))) %>% 
        ungroup %>% 
        DT::datatable(
                caption = tags$caption(
                        style = "caption-side: top; text-align: left; font-size: 18px; font-weight: bold; padding: 10px;",
                        "Distribution CV (0s exclus)"
                ),
                rownames = FALSE,
                extensions = c("Buttons", "RowGroup", "FixedHeader"),
                options = list(buttons        = list(
                      "copy",
                      list(extend = "excel", filename = "tableau distribution CV (0s exclus)")
                      # Also available: "csv", "pdf", "print"
                )),
        )


```

```{r}
data = data %>% mutate(tmp_depuis_diag = age-age_diag_vhd)
```

## Traitement
```{r}
# H/F, Fibrose , Comorbidités etc, TT du VHB (normalement proche de 100%) > A noter, je me suis trompée dans le tableau, je n'ai pas noté un traitement par Lamivudine dans NUC alors qu'il s'agit d'un analogue nucléosidique.
```
### traitement VHD vs sexe
```{r traitement VHD 1eme ligne vs sexe}
data %>% 
        group_by(vhd_traitement_1) %>% 
        count(sexe) %>% 
        mutate("% par traitement" = round(n*100/sum(n),2)) %>% 
        ungroup() %>% 
        mutate("% touts tt confondus" = round(n*100/sum(n),2),
               vhd_traitement_1 = case_when(
                        vhd_traitement_1 == "Bulévirtide" ~ paste0(vhd_traitement_1," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_1) %>%
                                                                filter(vhd_traitement_1=="Bulévirtide") %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
                        vhd_traitement_1 == "Bulévirtide en association pegIFNa" ~ paste0(vhd_traitement_1," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_1) %>%
                                                                filter(vhd_traitement_1=="Bulévirtide en association pegIFNa") %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
                        vhd_traitement_1 == "PegIFNa" ~ paste0(vhd_traitement_1," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_1) %>%
                                                                filter(vhd_traitement_1=="PegIFNa") %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
                        vhd_traitement_1 == "Non traité" ~ paste0(vhd_traitement_1," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_1) %>%
                                                                filter(vhd_traitement_1=="Non traité") %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
                        is.na(vhd_traitement_1) ~ paste0("Données manquantes"," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_1) %>%
                                                                filter(is.na(vhd_traitement_1)) %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
               )
               ) %>% 
        DT::datatable(
                caption = tags$caption(
                        style = "caption-side: top; text-align: left; 
                        font-size: 18px; font-weight: bold; padding: 10px;",
                        "traitement VHD 1ère ligne vs sexe"
                ),
                rownames = FALSE,
                extensions = c("Buttons", "RowGroup", "FixedHeader"),
                options = list(buttons = list("copy",  
                                        list(extend = "excel", filename = "vhd_traitement_1 vs sexe")
		),
                        rowGroup = list(
                                dataSrc = match("vhd_traitement_1", names(.))-1   
                        ),
                        columnDefs = list(
                                list(
                                        visible = FALSE,
                                        targets = match("vhd_traitement_1", names(.)) -1
                                )
                        )
                )
        )
```
```{r traitement VHD 2eme ligne vs sexe}
data %>% 
        group_by(vhd_traitement_2) %>% 
        count(sexe) %>% 
        mutate("% par traitement" = round(n*100/sum(n),2)) %>% 
        ungroup() %>% 
        mutate("% touts tt confondus" = round(n*100/sum(n),2),
               vhd_traitement_2 = case_when(
                        vhd_traitement_2 == "Bulévirtide" ~ paste0(vhd_traitement_2," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_2) %>%
                                                                filter(vhd_traitement_2=="Bulévirtide") %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
                        vhd_traitement_2 == "Bulévirtide en association pegIFNa" ~ paste0(vhd_traitement_2," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_2) %>%
                                                                filter(vhd_traitement_2=="Bulévirtide en association pegIFNa") %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
                        vhd_traitement_2 == "PegIFNa" ~ paste0(vhd_traitement_2," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_2) %>%
                                                                filter(vhd_traitement_2=="PegIFNa") %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
                        is.na(vhd_traitement_2) ~ paste0("Données manquantes"," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_2) %>%
                                                                filter(is.na(vhd_traitement_2)) %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
               )
               ) %>% 
        DT::datatable(
                caption = tags$caption(
                        style = "caption-side: top; text-align: left; 
                        font-size: 18px; font-weight: bold; padding: 10px;",
                        "traitement VHD 1ème ligne vs sexe"
                ),
                rownames = FALSE,
                extensions = c("Buttons", "RowGroup", "FixedHeader"),
                options = list(buttons = list("copy",  
                                        list(extend = "excel", filename = "vhd_traitement_2 vs sexe")
		),
                        rowGroup = list(
                                dataSrc = match("vhd_traitement_2", names(.))-1   
                        ),
                        columnDefs = list(
                                list(
                                        visible = FALSE,
                                        targets = match("vhd_traitement_2", names(.)) -1
                                )
                        )
                )
        )
```
```{r traitement VHD 3eme ligne vs sexe}
data %>% 
        group_by(vhd_traitement_3) %>% 
        count(sexe) %>% 
        mutate("% par traitement" = round(n*100/sum(n),2)) %>% 
        ungroup() %>% 
        mutate("% touts tt confondus" = round(n*100/sum(n),2),
               vhd_traitement_3 = case_when(
                        vhd_traitement_3 == "Bulévirtide" ~ paste0(vhd_traitement_3," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_3) %>%
                                                                filter(vhd_traitement_3=="Bulévirtide") %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
                        vhd_traitement_3 == "Bulévirtide en association pegIFNa" ~ paste0(vhd_traitement_3," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_3) %>%
                                                                filter(vhd_traitement_3=="Bulévirtide en association pegIFNa") %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
                        is.na(vhd_traitement_3) ~ paste0("Données manquantes"," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_3) %>%
                                                                filter(is.na(vhd_traitement_3)) %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
               )
               ) %>% 
        DT::datatable(
                caption = tags$caption(
                        style = "caption-side: top; text-align: left; 
                        font-size: 18px; font-weight: bold; padding: 10px;",
                        "traitement VHD 3ème ligne vs sexe"
                ),
                rownames = FALSE,
                extensions = c("Buttons", "RowGroup", "FixedHeader"),
                options = list(buttons = list("copy",  
                                        list(extend = "excel", filename = "vhd_traitement_3 vs sexe")
		),
                        rowGroup = list(
                                dataSrc = match("vhd_traitement_3", names(.))-1   
                        ),
                        columnDefs = list(
                                list(
                                        visible = FALSE,
                                        targets = match("vhd_traitement_3", names(.)) -1
                                )
                        )
                )
        )
```
### traitement VHD vs score fibrose
```{r traitement VHD 1eme ligne vs score fibrose}
data %>% 
        group_by(vhd_traitement_1) %>% 
        count(score_fibrose) %>% 
        mutate("% par traitement" = round(n*100/sum(n),2)) %>% 
        ungroup() %>% 
        mutate("% touts tt confondus" = round(n*100/sum(n),2),
               vhd_traitement_1 = case_when(
                        vhd_traitement_1 == "Bulévirtide" ~ paste0(vhd_traitement_1," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_1) %>%
                                                                filter(vhd_traitement_1=="Bulévirtide") %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
                        vhd_traitement_1 == "Bulévirtide en association pegIFNa" ~ paste0(vhd_traitement_1," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_1) %>%
                                                                filter(vhd_traitement_1=="Bulévirtide en association pegIFNa") %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
                        vhd_traitement_1 == "PegIFNa" ~ paste0(vhd_traitement_1," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_1) %>%
                                                                filter(vhd_traitement_1=="PegIFNa") %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
                        vhd_traitement_1 == "Non traité" ~ paste0(vhd_traitement_1," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_1) %>%
                                                                filter(vhd_traitement_1=="Non traité") %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
                        is.na(vhd_traitement_1) ~ paste0("Données manquantes"," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_1) %>%
                                                                filter(is.na(vhd_traitement_1)) %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
               )
               ) %>% 
        DT::datatable(
                caption = tags$caption(
                        style = "caption-side: top; text-align: left; 
                        font-size: 18px; font-weight: bold; padding: 10px;",
                        "traitement VHD 1ère ligne vs score fibrose"
                ),
                rownames = FALSE,
                extensions = c("Buttons", "RowGroup", "FixedHeader"),
                options = list(buttons = list("copy",  
                                        list(extend = "excel", filename = "vhd_traitement_1 vs score fibrose")
		),
                        rowGroup = list(
                                dataSrc = match("vhd_traitement_1", names(.))-1   
                        ),
                        columnDefs = list(
                                list(
                                        visible = FALSE,
                                        targets = match("vhd_traitement_1", names(.)) -1
                                )
                        )
                )
        )
```
```{r traitement VHD 2eme ligne vs score fibrose}
data %>% 
        group_by(vhd_traitement_2) %>% 
        count(score_fibrose) %>% 
        mutate("% par traitement" = round(n*100/sum(n),2)) %>% 
        ungroup() %>% 
        mutate("% touts tt confondus" = round(n*100/sum(n),2),
               vhd_traitement_2 = case_when(
                        vhd_traitement_2 == "Bulévirtide" ~ paste0(vhd_traitement_2," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_2) %>%
                                                                filter(vhd_traitement_2=="Bulévirtide") %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
                        vhd_traitement_2 == "Bulévirtide en association pegIFNa" ~ paste0(vhd_traitement_2," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_2) %>%
                                                                filter(vhd_traitement_2=="Bulévirtide en association pegIFNa") %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
                        vhd_traitement_2 == "PegIFNa" ~ paste0(vhd_traitement_2," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_2) %>%
                                                                filter(vhd_traitement_2=="PegIFNa") %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
                        is.na(vhd_traitement_2) ~ paste0("Données manquantes"," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_2) %>%
                                                                filter(is.na(vhd_traitement_2)) %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
               )
               ) %>% 
        DT::datatable(
                caption = tags$caption(
                        style = "caption-side: top; text-align: left; 
                        font-size: 18px; font-weight: bold; padding: 10px;",
                        "traitement VHD 2eme ligne vs score fibrose"
                ),
                rownames = FALSE,
                extensions = c("Buttons", "RowGroup", "FixedHeader"),
                options = list(buttons = list("copy",  
                                        list(extend = "excel", filename = "vhd_traitement_2 vs score fibrose")
		),
                        rowGroup = list(
                                dataSrc = match("vhd_traitement_2", names(.))-1   
                        ),
                        columnDefs = list(
                                list(
                                        visible = FALSE,
                                        targets = match("vhd_traitement_2", names(.)) -1
                                )
                        )
                )
        )
```
```{r traitement VHD 3eme ligne vs score fibrose}
data %>% 
        group_by(vhd_traitement_3) %>% 
        count(score_fibrose) %>% 
        mutate("% par traitement" = round(n*100/sum(n),2)) %>% 
        ungroup() %>% 
        mutate("% touts tt confondus" = round(n*100/sum(n),2),
               vhd_traitement_3 = case_when(
                        vhd_traitement_3 == "Bulévirtide" ~ paste0(vhd_traitement_3," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_3) %>%
                                                                filter(vhd_traitement_3=="Bulévirtide") %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
                        vhd_traitement_3 == "Bulévirtide en association pegIFNa" ~ paste0(vhd_traitement_3," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_3) %>%
                                                                filter(vhd_traitement_3=="Bulévirtide en association pegIFNa") %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
                        is.na(vhd_traitement_3) ~ paste0("Données manquantes"," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_3) %>%
                                                                filter(is.na(vhd_traitement_3)) %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
               )
               ) %>% 
        DT::datatable(
                caption = tags$caption(
                        style = "caption-side: top; text-align: left; 
                        font-size: 18px; font-weight: bold; padding: 10px;",
                        "traitement VHD 3ème ligne vs score fibrose"
                ),
                rownames = FALSE,
                extensions = c("Buttons", "RowGroup", "FixedHeader"),
                options = list(buttons = list("copy",  
                                        list(extend = "excel", filename = "vhd_traitement_3 vs score fibrose")
		),
                        rowGroup = list(
                                dataSrc = match("vhd_traitement_3", names(.))-1   
                        ),
                        columnDefs = list(
                                list(
                                        visible = FALSE,
                                        targets = match("vhd_traitement_3", names(.)) -1
                                )
                        )
                )
        )
```
### traitement VHD vs cirrhose
```{r traitement VHD 1eme ligne vs cirrhose}
data %>% 
        group_by(vhd_traitement_1) %>% 
        count(cirrhose_bin) %>% 
        mutate("% par traitement" = round(n*100/sum(n),2)) %>% 
        ungroup() %>% 
        mutate("% touts tt confondus" = round(n*100/sum(n),2),
               vhd_traitement_1 = case_when(
                        vhd_traitement_1 == "Bulévirtide" ~ paste0(vhd_traitement_1," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_1) %>%
                                                                filter(vhd_traitement_1=="Bulévirtide") %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
                        vhd_traitement_1 == "Bulévirtide en association pegIFNa" ~ paste0(vhd_traitement_1," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_1) %>%
                                                                filter(vhd_traitement_1=="Bulévirtide en association pegIFNa") %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
                        vhd_traitement_1 == "PegIFNa" ~ paste0(vhd_traitement_1," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_1) %>%
                                                                filter(vhd_traitement_1=="PegIFNa") %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
                        vhd_traitement_1 == "Non traité" ~ paste0(vhd_traitement_1," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_1) %>%
                                                                filter(vhd_traitement_1=="Non traité") %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
                        is.na(vhd_traitement_1) ~ paste0("Données manquantes"," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_1) %>%
                                                                filter(is.na(vhd_traitement_1)) %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
               )
               ) %>% 
        DT::datatable(
                caption = tags$caption(
                        style = "caption-side: top; text-align: left; 
                        font-size: 18px; font-weight: bold; padding: 10px;",
                        "traitement VHD 1ère ligne vs cirrhose"
                ),
                rownames = FALSE,
                extensions = c("Buttons", "RowGroup", "FixedHeader"),
                options = list(buttons = list("copy",  
                                        list(extend = "excel", filename = "vhd_traitement_1 vs cirrhose")
		),
                        rowGroup = list(
                                dataSrc = match("vhd_traitement_1", names(.))-1   
                        ),
                        columnDefs = list(
                                list(
                                        visible = FALSE,
                                        targets = match("vhd_traitement_1", names(.)) -1
                                )
                        )
                )
        )
```
```{r traitement VHD 2eme ligne vs cirrhose}
data %>% 
        group_by(vhd_traitement_2) %>% 
        count(cirrhose_bin) %>% 
        mutate("% par traitement" = round(n*100/sum(n),2)) %>% 
        ungroup() %>% 
        mutate("% touts tt confondus" = round(n*100/sum(n),2),
               vhd_traitement_2 = case_when(
                        vhd_traitement_2 == "Bulévirtide" ~ paste0(vhd_traitement_2," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_2) %>%
                                                                filter(vhd_traitement_2=="Bulévirtide") %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
                        vhd_traitement_2 == "Bulévirtide en association pegIFNa" ~ paste0(vhd_traitement_2," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_2) %>%
                                                                filter(vhd_traitement_2=="Bulévirtide en association pegIFNa") %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
                        vhd_traitement_2 == "PegIFNa" ~ paste0(vhd_traitement_2," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_2) %>%
                                                                filter(vhd_traitement_2=="PegIFNa") %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
                        is.na(vhd_traitement_2) ~ paste0("Données manquantes"," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_2) %>%
                                                                filter(is.na(vhd_traitement_2)) %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
               )
               ) %>% 
        DT::datatable(
                caption = tags$caption(
                        style = "caption-side: top; text-align: left; 
                        font-size: 18px; font-weight: bold; padding: 10px;",
                        "traitement VHD 2eme ligne vs cirrhose"
                ),
                rownames = FALSE,
                extensions = c("Buttons", "RowGroup", "FixedHeader"),
                options = list(buttons = list("copy",  
                                        list(extend = "excel", filename = "vhd_traitement_2 vs cirrhose")
		),
                        rowGroup = list(
                                dataSrc = match("vhd_traitement_2", names(.))-1   
                        ),
                        columnDefs = list(
                                list(
                                        visible = FALSE,
                                        targets = match("vhd_traitement_2", names(.)) -1
                                )
                        )
                )
        )
```
```{r traitement VHD 3eme ligne vs cirrhose}
data %>% 
        group_by(vhd_traitement_3) %>% 
        count(cirrhose_bin) %>% 
        mutate("% par traitement" = round(n*100/sum(n),2)) %>% 
        ungroup() %>% 
        mutate("% touts tt confondus" = round(n*100/sum(n),2),
               vhd_traitement_3 = case_when(
                        vhd_traitement_3 == "Bulévirtide" ~ paste0(vhd_traitement_3," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_3) %>%
                                                                filter(vhd_traitement_3=="Bulévirtide") %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
                        vhd_traitement_3 == "Bulévirtide en association pegIFNa" ~ paste0(vhd_traitement_3," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_3) %>%
                                                                filter(vhd_traitement_3=="Bulévirtide en association pegIFNa") %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
                        is.na(vhd_traitement_3) ~ paste0("Données manquantes"," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_3) %>%
                                                                filter(is.na(vhd_traitement_3)) %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
               )
               ) %>% 
        DT::datatable(
                caption = tags$caption(
                        style = "caption-side: top; text-align: left; 
                        font-size: 18px; font-weight: bold; padding: 10px;",
                        "traitement VHD 3ème ligne vs cirrhose"
                ),
                rownames = FALSE,
                extensions = c("Buttons", "RowGroup", "FixedHeader"),
                options = list(buttons = list("copy",  
                                        list(extend = "excel", filename = "vhd_traitement_3 vs cirrhose")
		),
                        rowGroup = list(
                                dataSrc = match("vhd_traitement_3", names(.))-1   
                        ),
                        columnDefs = list(
                                list(
                                        visible = FALSE,
                                        targets = match("vhd_traitement_3", names(.)) -1
                                )
                        )
                )
        )
```
### traitement VHD vs tt VHB
```{r traitement VHD 1eme ligne vs tt VHB}
data %>% 
        group_by(vhd_traitement_1) %>% 
        count(vhb_traitement) %>% 
        mutate("% par traitement" = round(n*100/sum(n),2)) %>% 
        ungroup() %>% 
        mutate("% touts tt confondus" = round(n*100/sum(n),2),
               vhd_traitement_1 = case_when(
                        vhd_traitement_1 == "Bulévirtide" ~ paste0(vhd_traitement_1," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_1) %>%
                                                                filter(vhd_traitement_1=="Bulévirtide") %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
                        vhd_traitement_1 == "Bulévirtide en association pegIFNa" ~ paste0(vhd_traitement_1," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_1) %>%
                                                                filter(vhd_traitement_1=="Bulévirtide en association pegIFNa") %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
                        vhd_traitement_1 == "PegIFNa" ~ paste0(vhd_traitement_1," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_1) %>%
                                                                filter(vhd_traitement_1=="PegIFNa") %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
                        vhd_traitement_1 == "Non traité" ~ paste0(vhd_traitement_1," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_1) %>%
                                                                filter(vhd_traitement_1=="Non traité") %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
                        is.na(vhd_traitement_1) ~ paste0("Données manquantes"," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_1) %>%
                                                                filter(is.na(vhd_traitement_1)) %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
               )
               ) %>% 
        DT::datatable(
                caption = tags$caption(
                        style = "caption-side: top; text-align: left; 
                        font-size: 18px; font-weight: bold; padding: 10px;",
                        "traitement VHD 1ère ligne vs tt vhb"
                ),
                rownames = FALSE,
                extensions = c("Buttons", "RowGroup", "FixedHeader"),
                options = list(buttons = list("copy",  
                                        list(extend = "excel", filename = "vhd_traitement_1 vs tt vhb")
		),
                        rowGroup = list(
                                dataSrc = match("vhd_traitement_1", names(.)) -1 
                        ),
                        columnDefs = list(
                                list(
                                        visible = FALSE,
                                        targets = match("vhd_traitement_1", names(.)) -1
                                )
                        )
                )
        )
```
```{r traitement VHD 2eme ligne vs tt VHB}
data %>% 
        group_by(vhd_traitement_2) %>% 
        count(vhb_traitement) %>% 
        mutate("% par traitement" = round(n*100/sum(n),2)) %>% 
        ungroup() %>% 
        mutate("% touts tt confondus" = round(n*100/sum(n),2),
               vhd_traitement_2 = case_when(
                        vhd_traitement_2 == "Bulévirtide" ~ paste0(vhd_traitement_2," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_2) %>%
                                                                filter(vhd_traitement_2=="Bulévirtide") %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
                        vhd_traitement_2 == "Bulévirtide en association pegIFNa" ~ paste0(vhd_traitement_2," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_2) %>%
                                                                filter(vhd_traitement_2=="Bulévirtide en association pegIFNa") %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
                        vhd_traitement_2 == "PegIFNa" ~ paste0(vhd_traitement_2," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_2) %>%
                                                                filter(vhd_traitement_2=="PegIFNa") %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
                        is.na(vhd_traitement_2) ~ paste0("Données manquantes"," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_2) %>%
                                                                filter(is.na(vhd_traitement_2)) %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
               )
               ) %>% 
        DT::datatable(
                caption = tags$caption(
                        style = "caption-side: top; text-align: left; 
                        font-size: 18px; font-weight: bold; padding: 10px;",
                        "traitement VHD 2eme ligne vs tt vhb"
                ),
                rownames = FALSE,
                extensions = c("Buttons", "RowGroup", "FixedHeader"),
                options = list(buttons = list("copy",  
                                        list(extend = "excel", filename = "vhd_traitement_2 vs tt vhb")
		),
                        rowGroup = list(
                                dataSrc = match("vhd_traitement_2", names(.))-1   
                        ),
                        columnDefs = list(
                                list(
                                        visible = FALSE,
                                        targets = match("vhd_traitement_2", names(.)) -1
                                )
                        )
                )
        )
```
```{r traitement VHD 3eme ligne vs tt VHB}
data %>% 
        group_by(vhd_traitement_3) %>% 
        count(vhb_traitement) %>% 
        mutate("% par traitement" = round(n*100/sum(n),2)) %>% 
        ungroup() %>% 
        mutate("% touts tt confondus" = round(n*100/sum(n),2),
               vhd_traitement_3 = case_when(
                        vhd_traitement_3 == "Bulévirtide" ~ paste0(vhd_traitement_3," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_3) %>%
                                                                filter(vhd_traitement_3=="Bulévirtide") %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
                        vhd_traitement_3 == "Bulévirtide en association pegIFNa" ~ paste0(vhd_traitement_3," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_3) %>%
                                                                filter(vhd_traitement_3=="Bulévirtide en association pegIFNa") %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
                        is.na(vhd_traitement_3) ~ paste0("Données manquantes"," - (",
                                                        round((data %>%
                                                                count(vhd_traitement_3) %>%
                                                                filter(is.na(vhd_traitement_3)) %>%
                                                                pull(n)
                                                               )*100/367,2),"%)"),
               )
               ) %>% 
        DT::datatable(
                caption = tags$caption(
                        style = "caption-side: top; text-align: left; 
                        font-size: 18px; font-weight: bold; padding: 10px;",
                        "traitement VHD 3ème ligne vs tt vhb"
                ),
                rownames = FALSE,
                extensions = c("Buttons", "RowGroup", "FixedHeader"),
                options = list(buttons = list("copy",  
                                        list(extend = "excel", filename = "vhd_traitement_3 vs tt vhb")
		),
                        rowGroup = list(
                                dataSrc = match("vhd_traitement_3", names(.))-1   
                        ),
                        columnDefs = list(
                                list(
                                        visible = FALSE,
                                        targets = match("vhd_traitement_3", names(.)) -1
                                )
                        )
                )
        )
```
### Reduction cv vhd à s24
```{r traitement VHD 3eme ligne vs Reduction cv vhd à s24}
data %>% count(reduction_cv_vhd_1) %>% mutate("%" = round(n*100/sum(n),2),
                                              "% sans NA" = round(n*100/(sum(n)-298),2),
                                              "% sans NA" = ifelse(`% sans NA`>100,NA,`% sans NA`))  %>% 
        DT::datatable(
                caption = tags$caption(
                        style = "caption-side: top; text-align: left; 
                        font-size: 18px; font-weight: bold; padding: 10px;",
                        "traitement VHD 1ère ligne vs reduction cv vhd à s24"
                ),
                rownames = FALSE,
                extensions = c("Buttons", "RowGroup", "FixedHeader"),
                options = list(buttons = list("copy",  
                                        list(extend = "excel", filename = "vhd_traitement_1 vs reduction cv vhd à s24")
		)))

data %>% count(reduction_cv_vhd_2) %>% mutate("%" = round(n*100/sum(n),2),
                                              "% sans NA" = round(n*100/(sum(n)-344),2),
                                              "% sans NA" = ifelse(`% sans NA`>100,NA,`% sans NA`)) %>% 
        DT::datatable(
                caption = tags$caption(
                        style = "caption-side: top; text-align: left; 
                        font-size: 18px; font-weight: bold; padding: 10px;",
                        "traitement VHD 2ème ligne vs reduction cv vhd à s24"
                ),
                rownames = FALSE,
                extensions = c("Buttons", "RowGroup", "FixedHeader"),
                options = list(buttons = list("copy",  
                                        list(extend = "excel", filename = "vhd_traitement_2 vs reduction cv vhd à s24")
		)))

data %>% count(reduction_cv_vhd_3) %>% mutate("%" = round(n*100/sum(n),2),
                                              "% sans NA" = round(n*100/(sum(n)-356),2),
                                              "% sans NA" = ifelse(`% sans NA`>100,NA,`% sans NA`)) %>% 
        DT::datatable(
                caption = tags$caption(
                        style = "caption-side: top; text-align: left; 
                        font-size: 18px; font-weight: bold; padding: 10px;",
                        "traitement VHD 3ème ligne vs reduction cv vhd à s24"
                ),
                rownames = FALSE,
                extensions = c("Buttons", "RowGroup", "FixedHeader"),
                options = list(buttons = list("copy",  
                                        list(extend = "excel", filename = "vhd_traitement_3 vs reduction cv vhd à s24")
		)))
```
### Lignes de traitement
```{r lignes de traitement}
data %>% 
        count(vhd_traitement_1,vhd_traitement_2,vhd_traitement_3) %>%
        arrange(desc(n)) %>% 
        DT::datatable(
                caption = tags$caption(
                        style = "caption-side: top; text-align: left; 
                        font-size: 18px; font-weight: bold; padding: 10px;",
                        "Lignes de traitement"
                ),
                rownames = FALSE,
                extensions = c("Buttons", "RowGroup", "FixedHeader"),
                options = list(buttons = list("copy",  
                                        list(extend = "excel", filename = "Lignes de traitement")
		)))

```
### Complications
```{r tableaux complications}
ei1 = data %>% select(starts_with("ei") & ends_with("1")) %>% names

data %>% 
        count(vhd_traitement_1,pick(all_of(ei1))) %>% 
        arrange(desc(n)) %>% mutate("%" = round(n*100/sum(n),2)) %>% 
        group_by(vhd_traitement_1) %>% 
        mutate("% by traitement" = round(n*100/sum(n),2)) %>% 
        DT::datatable(
                caption = tags$caption(
                        style = "caption-side: top; text-align: left; 
                        font-size: 18px; font-weight: bold; padding: 10px;",
                        "Complications vhd_traitement_1"
                ),
                rownames = FALSE,
                extensions = c("Buttons", "RowGroup", "FixedHeader"),
                options = list(buttons = list("copy",  
                                        list(extend = "excel", filename = "Complications vhd_traitement_1")
		)))

data %>% 
        count(vhd_traitement_2,pick(all_of(ei1))) %>% 
        arrange(desc(n)) %>% mutate("%" = round(n*100/sum(n),2)) %>% 
        group_by(vhd_traitement_2) %>% 
        mutate("% by traitement" = round(n*100/sum(n),2)) %>% 
        DT::datatable(
                caption = tags$caption(
                        style = "caption-side: top; text-align: left; 
                        font-size: 18px; font-weight: bold; padding: 10px;",
                        "Complications vhd_traitement_2"
                ),
                rownames = FALSE,
                extensions = c("Buttons", "RowGroup", "FixedHeader"),
                options = list(buttons = list("copy",  
                                        list(extend = "excel", filename = "Complications vhd_traitement_2")
		)))

data %>% 
        count(vhd_traitement_3,pick(all_of(ei1))) %>% 
        arrange(desc(n)) %>% mutate("%" = round(n*100/sum(n),2)) %>% 
        group_by(vhd_traitement_3) %>% 
        mutate("% by traitement" = round(n*100/sum(n),2)) %>% 
        DT::datatable(
                caption = tags$caption(
                        style = "caption-side: top; text-align: left; 
                        font-size: 18px; font-weight: bold; padding: 10px;",
                        "Complications vhd_traitement_3"
                ),
                rownames = FALSE,
                extensions = c("Buttons", "RowGroup", "FixedHeader"),
                options = list(buttons = list("copy",  
                                        list(extend = "excel", filename = "Complications vhd_traitement_3")
		)))


```

